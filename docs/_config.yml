title: VideoToolbox Remote
description: "Remote VideoToolbox for FFmpeg: use a Mac or Apple Silicon system over LAN as a hardware H.264/HEVC encode, decode, and transcoding server for Linux, Windows, and macOS clients."
current_release: "v0.6.3"
latest_release_url: "https://github.com/davelindo/videotoolbox_remote/releases/latest"
repository_url: "https://github.com/davelindo/videotoolbox_remote"
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false
baseurl: "/videotoolbox_remote"
url: "https://davelindo.github.io"
plugins:
  - jekyll-sitemap
defaults:
  - scope:
      path: ""
    values:
      layout: default
